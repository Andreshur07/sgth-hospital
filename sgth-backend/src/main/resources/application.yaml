spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/hospital_rrhh
    username: postgres
    password: "ejnhfnif0925"

  jpa:
    hibernate:
      ddl-auto: validate

    show-sql: true

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true
    baseline-on-migrate: true
    locations: classpath:db/migration

server:
  port: 8080

logging:
  level:
    org.springframework.security: DEBUG